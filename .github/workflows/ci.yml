name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "20"

      - name: Install Dependencies
        run: yarn install

      - name: Run Tests
        run: yarn run test

      - name: Archive Artifacts
        uses: actions/upload-artifact@v2
        with:
          name: coverage
          path: __tests__/coverage

      - name: Run Build
        run: yarn build

      - name: Archive Artifacts
        uses: actions/upload-artifact@v2
        with:
          name: build
          path: dist

  deploy:
    runs-on: ubuntu-latest
    needs: build-and-test
    steps:
      # - name: Upload File
      #   uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      #   with:
      #     server: us-east-1.sftpcloud.io
      #     username: 9122c0e7880e427fa2fb582c96358759
      #     password: V18eYHWakQtypWhI8ZBFDaRGHW85664R
      #     local-dir: ./dist/
      #     server-dir: ./build/

      - name: Install Render CLI
        run: |
          curl -fsSL https://render.com/install | bash

      - name: Login to Render
        run: |
          echo rnd_uqR0LVeqtlYwwiUy44fl8MoLdvhZ | render login --api-key -

      - name: Build Docker Image
        run: docker build -t my-app:latest .

      - name: Deploy to Render
        run: render deploy --service ci01 --image my-app:latest

      - name: Finalize Job
        run: echo "Pipeline completed successfully"
